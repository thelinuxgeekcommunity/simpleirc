apply plugin: 'com.android.application'
// Load the keystore properties from the root of the project
def keystorePropertiesFile = rootProject.file("keystore.properties")
def keystoreProperties = new Properties()
keystoreProperties.load(new FileInputStream(keystorePropertiesFile))

android {
    signingConfigs {
        KeyConfig {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile file(keystoreProperties['storeFile'])
            storePassword keystoreProperties['storePassword']
        }
    }
    lintOptions {
        abortOnError false
    }
    compileSdkVersion 22
    buildToolsVersion "25.0.0"
    defaultConfig {
        applicationId 'tk.jordynsmediagroup.simpleirc'
        minSdkVersion 11 // Android 3.0 Honeycomb
        targetSdkVersion 22 // Android 6.0 Marshmallow
    }
    buildTypes {
        release {
            // Build config for a generic release build
            minifyEnabled false
            signingConfig signingConfigs.KeyConfig
            pseudoLocalesEnabled true
            zipAlignEnabled true
        }
        debug {
            // Build config for a debug build
            applicationIdSuffix = '.debug'
            jniDebuggable true
            renderscriptDebuggable true
            minifyEnabled true
            zipAlignEnabled true
            applicationIdSuffix '.debug'
        }
        fdroid {
            // Build config for the F-droid marketplace
            debuggable false
            jniDebuggable false
            renderscriptDebuggable false
            minifyEnabled false
            pseudoLocalesEnabled true
            applicationIdSuffix '.fdroid'
            zipAlignEnabled true
            signingConfig signingConfigs.KeyConfig
        }
    }
    productFlavors {
        normal {
            applicationId 'tk.jordynsmediagroup.simpleirc'
        }
    }
}

dependencies {
    // Dependencies for the entire project
    compile project(':viewPagerIndicator')
    compile project(':memorizingTrustManager')
    compile project(':colorPickerView')
    compile 'com.android.support:support-v4:22.0.0'
    compile 'com.android.support:appcompat-v7:22.1.0'
}
